// Preload script
// This file is needed because we're using contextIsolation: false
// It's kept minimal for now but can be expanded if needed

window.addEventListener('DOMContentLoaded', () => {
    console.log('Preload script loaded');
  });
